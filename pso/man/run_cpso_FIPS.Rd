% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_cpso_FIPS.R
\name{run_cpso_FIPS}
\alias{run_cpso_FIPS}
\title{Fully Informed Particle Swarm Optimization}
\usage{
run_cpso_FIPS(
  objective_function,
  N,
  xmin,
  xmax,
  max_iter,
  w = 1/(2 * log(2)),
  c = 0.5 + log(2),
  rho = NULL
)
}
\arguments{
\item{objective_function}{A function to be minimized, which takes a numeric vector as input and returns a numeric value.}

\item{N}{Integer. The number of particles in the swarm.}

\item{xmin}{Numeric vector. The lower bounds of the search space.}

\item{xmax}{Numeric vector. The upper bounds of the search space.}

\item{max_iter}{Integer. The maximum number of iterations.}

\item{w}{Numeric. The inertia weight. Defaults to \eqn{\frac{1}{2 \log 2}}.}

\item{c}{Numeric. The acceleration coefficient. Defaults to \eqn{0.5 + \log 2}.}

\item{rho}{Numeric. The proportion of the population that acts as informants for each particle. Defaults to \eqn{\varrho = 1 - \Big(1 - \dfrac{1}{N}\Big)^{d}}}
}
\value{
A list containing the best position found and the corresponding fitness value.
}
\description{
This function runs the CPSO algorithm to find the minimum of the provided objective function.
It initializes a swarm of particles, updates their velocities and positions over iterations,
and returns the best solution found.
}
\details{
The CPSO algorithm follows the Fully Informed Particle Swarm (FIPS) variant of PSO.
The velocity update equation is:
\deqn{
\mathbf{V}_{t+1}^{(i)} = w \cdot \mathbf{V}_{t}^{(i)} + c \cdot \sum_{j \in \mathcal{I}_i} r_j \cdot (\mathbf{p}_t^{(j)} - \mathbf{X}_t^{(i)})
}
where:
\itemize{
\item \eqn{w} is the inertia weight,
\item \eqn{c} is the acceleration coefficient,
\item \eqn{\mathcal{I}_i} is the set of informants for particle \eqn{i},
\item \eqn{\mathbf{p}_t^{(j)}} is the personal best position of the \eqn{j}-th informant,
\item \eqn{\mathbf{X}_t^{(i)}} is the current position of the \eqn{i}-th particle.
}
}
\examples{
objective_function <- function(x) sum(x^2)
result <- run_cpso(objective_function, N = 30, xmin = c(-5, -5), xmax = c(5, 5), max_iter = 100)
print(result)

}
\seealso{
\code{\link{initialize_swarm}}, \code{\link{update_velocity}}, \code{\link{update_position}}, \code{\link{evaluate_fitness}}
}
